{
  "name": "rhds-local-demo",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "scripts/build.js",
    "start": "npm run build && npx web-dev-server --open --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@rhds/elements": "^2.1.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "chalk": "^5.3.0",
    "fs-extra": "^11.2.0"
  }
}
